fragment VariantDetails on ProductVariant {
	id
	name
	quantityAvailable
	sku
	translation(languageCode: $languageCode) {
		name
	}
	pricing {
		price {
			gross {
				currency
				amount
			}
		}
	}
	metadata {
		key
		value
	}
}
