query ProductListByCategory($languageCode: LanguageCodeEnum!, $slug: String!, $channel: String!) {
	category(slug: $slug) {
		name
		description
		seoDescription
		seoTitle
		translation(languageCode: $languageCode) {
			name
			description
			seoTitle
			seoDescription
		}
		products(first: 100, channel: $channel) {
			edges {
				node {
					...ProductListItem
				}
			}
		}
	}
}
