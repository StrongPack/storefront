query PageGetBySlug($languageCode: LanguageCodeEnum!, $slug: String!) {
	page(slug: $slug) {
		id
		slug
		title
		seoTitle
		seoDescription
		content
		translation(languageCode: $languageCode) {
			id
			title
			seoTitle
			seoDescription
			content
		}
		metadata {
			key
			value
		}
	}
}
