fragment OrderDetails on Order {
	id
	number
	created
	total {
		gross {
			amount
			currency
		}
	}
	lines {
		variant {
			id
			name
			product {
				id
				name
				description
				slug
				translation(languageCode: $languageCode) {
					id
					name
					description
				}
				thumbnail {
					url
					alt
				}
				category {
					id
					name
					translation(languageCode: $languageCode) {
						id
						name
					}
				}
			}
			pricing {
				price {
					gross {
						amount
						currency
					}
				}
			}
		}
		quantity
	}
	paymentStatus
}
